interface:
  display_name: "Markdown to DOCX"
  short_description: "Build clean Word files from Markdown"
  default_prompt: "Use $markdown-to-docx to convert this Markdown draft into a clean DOCX without Markdown artifacts."
